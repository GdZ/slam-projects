/**
 * Automatically generated by Zserio Java extension version 2.12.0.
 * Generator setup: writerCode, pubsubCode, serviceCode, sqlCode.
 */

package nds.vehicle.metadata;

public class VehicleServiceCapabilities implements zserio.runtime.io.PackableWriter, zserio.runtime.PackableSizeOf
{
    public static final class ZserioPackingContext extends zserio.runtime.array.PackingContext
    {
        public ZserioPackingContext()
        {
            horizonServiceCapabilities_ = new zserio.runtime.array.DeltaContext();
        }

        public zserio.runtime.array.DeltaContext getHorizonServiceCapabilities()
        {
            return horizonServiceCapabilities_;
        }

        private zserio.runtime.array.DeltaContext horizonServiceCapabilities_;
    };

    public VehicleServiceCapabilities(
            nds.vehicle.metadata.VehicleServiceType type_)
    {
        this.type_ = type_;
    }

    public VehicleServiceCapabilities(zserio.runtime.io.BitStreamReader in,
            nds.vehicle.metadata.VehicleServiceType type_)
            throws java.io.IOException
    {
        this.type_ = type_;

        read(in);
    }

    public VehicleServiceCapabilities(zserio.runtime.array.PackingContext context, zserio.runtime.io.BitStreamReader in,
            nds.vehicle.metadata.VehicleServiceType type_)
            throws java.io.IOException
    {
        this.type_ = type_;

        read(context, in);
    }

    public int choiceTag()
    {
        switch (getType())
        {
        case HORIZON_SERVICE:
            return CHOICE_horizonServiceCapabilities;
        default:
            return UNDEFINED_CHOICE;
        }
    }

    @Override
    public void initPackingContext(zserio.runtime.array.PackingContext context)
    {
        final ZserioPackingContext zserioContext = context.cast();
        switch (getType())
        {
        case HORIZON_SERVICE:
            getHorizonServiceCapabilities().initPackingContext(zserioContext.getHorizonServiceCapabilities());
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }
    }

    @Override
    public int bitSizeOf()
    {
        return bitSizeOf(0);
    }

    @Override
    public int bitSizeOf(long bitPosition)
    {
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case HORIZON_SERVICE:
            endBitPosition += getHorizonServiceCapabilities().bitSizeOf(endBitPosition);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }

        return (int)(endBitPosition - bitPosition);
    }

    @Override
    public int bitSizeOf(zserio.runtime.array.PackingContext context, long bitPosition)
    {
        final ZserioPackingContext zserioContext = context.cast();
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case HORIZON_SERVICE:
            endBitPosition += getHorizonServiceCapabilities().bitSizeOf(zserioContext.getHorizonServiceCapabilities(), endBitPosition);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }

        return (int)(endBitPosition - bitPosition);
    }

    public nds.vehicle.metadata.VehicleServiceType getType()
    {
        return this.type_;
    }

    public nds.vehicle.metadata.HorizonServiceCapabilities getHorizonServiceCapabilities()
    {
        return (nds.vehicle.metadata.HorizonServiceCapabilities)objectChoice;
    }

    public void setHorizonServiceCapabilities(nds.vehicle.metadata.HorizonServiceCapabilities horizonServiceCapabilities_)
    {
        objectChoice = horizonServiceCapabilities_;
    }

    @Override
    public boolean equals(java.lang.Object obj)
    {
        if (obj instanceof VehicleServiceCapabilities)
        {
            final VehicleServiceCapabilities that = (VehicleServiceCapabilities)obj;

            return
                    ((this.type_ == null) ? that.type_ == null : this.type_.getValue() == that.type_.getValue()) &&
                    (
                        (objectChoice == null && that.objectChoice == null) ||
                        (objectChoice != null && objectChoice.equals(that.objectChoice))
                    );
        }

        return false;
    }

    @Override
    public int hashCode()
    {
        int result = zserio.runtime.HashCodeUtil.HASH_SEED;

        result = zserio.runtime.HashCodeUtil.calcHashCode(result, getType());
        if (objectChoice != null)
        {
            switch (getType())
            {
            case HORIZON_SERVICE:
                result = zserio.runtime.HashCodeUtil.calcHashCode(result, (nds.vehicle.metadata.HorizonServiceCapabilities)objectChoice);
                break;
            default:
                break;
            }
        }

        return result;
    }

    public void read(zserio.runtime.io.BitStreamReader in) throws java.io.IOException
    {
        switch (getType())
        {
        case HORIZON_SERVICE:
            objectChoice = new nds.vehicle.metadata.HorizonServiceCapabilities(in);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }
    }

    public void read(zserio.runtime.array.PackingContext context, zserio.runtime.io.BitStreamReader in)
            throws java.io.IOException
    {
        final ZserioPackingContext zserioContext = context.cast();
        switch (getType())
        {
        case HORIZON_SERVICE:
            objectChoice = new nds.vehicle.metadata.HorizonServiceCapabilities(zserioContext.getHorizonServiceCapabilities(), in);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }
    }

    @Override
    public long initializeOffsets()
    {
        return initializeOffsets(0);
    }

    @Override
    public long initializeOffsets(long bitPosition)
    {
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case HORIZON_SERVICE:
            {
                endBitPosition += getHorizonServiceCapabilities().bitSizeOf(endBitPosition);
            }
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }

        return endBitPosition;
    }

    @Override
    public long initializeOffsets(zserio.runtime.array.PackingContext context, long bitPosition)
    {
        final ZserioPackingContext zserioContext = context.cast();
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case HORIZON_SERVICE:
            {
                endBitPosition = getHorizonServiceCapabilities().initializeOffsets(zserioContext.getHorizonServiceCapabilities(), endBitPosition);
            }
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }

        return endBitPosition;
    }

    @Override
    public void write(zserio.runtime.io.BitStreamWriter out) throws java.io.IOException
    {
        switch (getType())
        {
        case HORIZON_SERVICE:
            getHorizonServiceCapabilities().write(out);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }
    }

    @Override
    public void write(zserio.runtime.array.PackingContext context, zserio.runtime.io.BitStreamWriter out)
            throws java.io.IOException
    {
        final ZserioPackingContext zserioContext = context.cast();
        switch (getType())
        {
        case HORIZON_SERVICE:
            getHorizonServiceCapabilities().write(zserioContext.getHorizonServiceCapabilities(), out);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice VehicleServiceCapabilities: " + getType() + "!");
        }
    }

    public static final int CHOICE_horizonServiceCapabilities = 0;
    public static final int UNDEFINED_CHOICE = -1;

    private final nds.vehicle.metadata.VehicleServiceType type_;
    private java.lang.Object objectChoice;
}
