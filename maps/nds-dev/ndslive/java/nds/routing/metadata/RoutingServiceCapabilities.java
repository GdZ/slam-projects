/**
 * Automatically generated by Zserio Java extension version 2.12.0.
 * Generator setup: writerCode, pubsubCode, serviceCode, sqlCode.
 */

package nds.routing.metadata;

public class RoutingServiceCapabilities implements zserio.runtime.io.PackableWriter, zserio.runtime.PackableSizeOf
{
    public static final class ZserioPackingContext extends zserio.runtime.array.PackingContext
    {
        public ZserioPackingContext()
        {
            routeServiceCapabilities_ = new nds.routing.metadata.RouteServiceCapabilities.ZserioPackingContext();
            rangeProjectionServiceCapabilities_ = new nds.routing.metadata.RangeProjectionServiceCapabilities.ZserioPackingContext();
        }

        public nds.routing.metadata.RouteServiceCapabilities.ZserioPackingContext getRouteServiceCapabilities()
        {
            return routeServiceCapabilities_;
        }

        public nds.routing.metadata.RangeProjectionServiceCapabilities.ZserioPackingContext getRangeProjectionServiceCapabilities()
        {
            return rangeProjectionServiceCapabilities_;
        }

        private nds.routing.metadata.RouteServiceCapabilities.ZserioPackingContext routeServiceCapabilities_;
        private nds.routing.metadata.RangeProjectionServiceCapabilities.ZserioPackingContext rangeProjectionServiceCapabilities_;
    };

    public RoutingServiceCapabilities(
            nds.routing.metadata.RoutingServiceType type_)
    {
        this.type_ = type_;
    }

    public RoutingServiceCapabilities(zserio.runtime.io.BitStreamReader in,
            nds.routing.metadata.RoutingServiceType type_)
            throws java.io.IOException
    {
        this.type_ = type_;

        read(in);
    }

    public RoutingServiceCapabilities(zserio.runtime.array.PackingContext context, zserio.runtime.io.BitStreamReader in,
            nds.routing.metadata.RoutingServiceType type_)
            throws java.io.IOException
    {
        this.type_ = type_;

        read(context, in);
    }

    public int choiceTag()
    {
        switch (getType())
        {
        case ROUTE_SERVICE:
            return CHOICE_routeServiceCapabilities;
        case RANGE_PROJECTION_SERVICE:
            return CHOICE_rangeProjectionServiceCapabilities;
        default:
            return UNDEFINED_CHOICE;
        }
    }

    @Override
    public void initPackingContext(zserio.runtime.array.PackingContext context)
    {
        final ZserioPackingContext zserioContext = context.cast();
        switch (getType())
        {
        case ROUTE_SERVICE:
            getRouteServiceCapabilities().initPackingContext(zserioContext.getRouteServiceCapabilities());
            break;
        case RANGE_PROJECTION_SERVICE:
            getRangeProjectionServiceCapabilities().initPackingContext(zserioContext.getRangeProjectionServiceCapabilities());
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }
    }

    @Override
    public int bitSizeOf()
    {
        return bitSizeOf(0);
    }

    @Override
    public int bitSizeOf(long bitPosition)
    {
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case ROUTE_SERVICE:
            endBitPosition += getRouteServiceCapabilities().bitSizeOf(endBitPosition);
            break;
        case RANGE_PROJECTION_SERVICE:
            endBitPosition += getRangeProjectionServiceCapabilities().bitSizeOf(endBitPosition);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }

        return (int)(endBitPosition - bitPosition);
    }

    @Override
    public int bitSizeOf(zserio.runtime.array.PackingContext context, long bitPosition)
    {
        final ZserioPackingContext zserioContext = context.cast();
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case ROUTE_SERVICE:
            endBitPosition += getRouteServiceCapabilities().bitSizeOf(zserioContext.getRouteServiceCapabilities(), endBitPosition);
            break;
        case RANGE_PROJECTION_SERVICE:
            endBitPosition += getRangeProjectionServiceCapabilities().bitSizeOf(zserioContext.getRangeProjectionServiceCapabilities(), endBitPosition);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }

        return (int)(endBitPosition - bitPosition);
    }

    public nds.routing.metadata.RoutingServiceType getType()
    {
        return this.type_;
    }

    public nds.routing.metadata.RouteServiceCapabilities getRouteServiceCapabilities()
    {
        return (nds.routing.metadata.RouteServiceCapabilities)objectChoice;
    }

    public void setRouteServiceCapabilities(nds.routing.metadata.RouteServiceCapabilities routeServiceCapabilities_)
    {
        objectChoice = routeServiceCapabilities_;
    }

    public nds.routing.metadata.RangeProjectionServiceCapabilities getRangeProjectionServiceCapabilities()
    {
        return (nds.routing.metadata.RangeProjectionServiceCapabilities)objectChoice;
    }

    public void setRangeProjectionServiceCapabilities(nds.routing.metadata.RangeProjectionServiceCapabilities rangeProjectionServiceCapabilities_)
    {
        objectChoice = rangeProjectionServiceCapabilities_;
    }

    @Override
    public boolean equals(java.lang.Object obj)
    {
        if (obj instanceof RoutingServiceCapabilities)
        {
            final RoutingServiceCapabilities that = (RoutingServiceCapabilities)obj;

            return
                    ((this.type_ == null) ? that.type_ == null : this.type_.getValue() == that.type_.getValue()) &&
                    (
                        (objectChoice == null && that.objectChoice == null) ||
                        (objectChoice != null && objectChoice.equals(that.objectChoice))
                    );
        }

        return false;
    }

    @Override
    public int hashCode()
    {
        int result = zserio.runtime.HashCodeUtil.HASH_SEED;

        result = zserio.runtime.HashCodeUtil.calcHashCode(result, getType());
        if (objectChoice != null)
        {
            switch (getType())
            {
            case ROUTE_SERVICE:
                result = zserio.runtime.HashCodeUtil.calcHashCode(result, (nds.routing.metadata.RouteServiceCapabilities)objectChoice);
                break;
            case RANGE_PROJECTION_SERVICE:
                result = zserio.runtime.HashCodeUtil.calcHashCode(result, (nds.routing.metadata.RangeProjectionServiceCapabilities)objectChoice);
                break;
            default:
                break;
            }
        }

        return result;
    }

    public void read(zserio.runtime.io.BitStreamReader in) throws java.io.IOException
    {
        switch (getType())
        {
        case ROUTE_SERVICE:
            objectChoice = new nds.routing.metadata.RouteServiceCapabilities(in);
            break;
        case RANGE_PROJECTION_SERVICE:
            objectChoice = new nds.routing.metadata.RangeProjectionServiceCapabilities(in);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }
    }

    public void read(zserio.runtime.array.PackingContext context, zserio.runtime.io.BitStreamReader in)
            throws java.io.IOException
    {
        final ZserioPackingContext zserioContext = context.cast();
        switch (getType())
        {
        case ROUTE_SERVICE:
            objectChoice = new nds.routing.metadata.RouteServiceCapabilities(zserioContext.getRouteServiceCapabilities(), in);
            break;
        case RANGE_PROJECTION_SERVICE:
            objectChoice = new nds.routing.metadata.RangeProjectionServiceCapabilities(zserioContext.getRangeProjectionServiceCapabilities(), in);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }
    }

    @Override
    public long initializeOffsets()
    {
        return initializeOffsets(0);
    }

    @Override
    public long initializeOffsets(long bitPosition)
    {
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case ROUTE_SERVICE:
            {
                endBitPosition = getRouteServiceCapabilities().initializeOffsets(endBitPosition);
            }
            break;
        case RANGE_PROJECTION_SERVICE:
            {
                endBitPosition = getRangeProjectionServiceCapabilities().initializeOffsets(endBitPosition);
            }
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }

        return endBitPosition;
    }

    @Override
    public long initializeOffsets(zserio.runtime.array.PackingContext context, long bitPosition)
    {
        final ZserioPackingContext zserioContext = context.cast();
        long endBitPosition = bitPosition;

        switch (getType())
        {
        case ROUTE_SERVICE:
            {
                endBitPosition = getRouteServiceCapabilities().initializeOffsets(zserioContext.getRouteServiceCapabilities(), endBitPosition);
            }
            break;
        case RANGE_PROJECTION_SERVICE:
            {
                endBitPosition = getRangeProjectionServiceCapabilities().initializeOffsets(zserioContext.getRangeProjectionServiceCapabilities(), endBitPosition);
            }
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }

        return endBitPosition;
    }

    @Override
    public void write(zserio.runtime.io.BitStreamWriter out) throws java.io.IOException
    {
        switch (getType())
        {
        case ROUTE_SERVICE:
            getRouteServiceCapabilities().write(out);
            break;
        case RANGE_PROJECTION_SERVICE:
            getRangeProjectionServiceCapabilities().write(out);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }
    }

    @Override
    public void write(zserio.runtime.array.PackingContext context, zserio.runtime.io.BitStreamWriter out)
            throws java.io.IOException
    {
        final ZserioPackingContext zserioContext = context.cast();
        switch (getType())
        {
        case ROUTE_SERVICE:
            getRouteServiceCapabilities().write(zserioContext.getRouteServiceCapabilities(), out);
            break;
        case RANGE_PROJECTION_SERVICE:
            getRangeProjectionServiceCapabilities().write(zserioContext.getRangeProjectionServiceCapabilities(), out);
            break;
        default:
            throw new zserio.runtime.ZserioError("No match in choice RoutingServiceCapabilities: " + getType() + "!");
        }
    }

    public static final int CHOICE_routeServiceCapabilities = 0;
    public static final int CHOICE_rangeProjectionServiceCapabilities = 1;
    public static final int UNDEFINED_CHOICE = -1;

    private final nds.routing.metadata.RoutingServiceType type_;
    private java.lang.Object objectChoice;
}
